# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# TODO: uncomment once deploying via `azd deploy` end-to-end, rather than the
# per-module `az acr build` + `az deployment group create` flow this project
# currently uses — see docs/03-Implementation-Guide.md Phase 2.

name: contoso-retail
metadata:
  template: contoso-retail@0.1.0

# services:
#   catalog-svc:
#     project: ./apps/catalog-svc
#     language: dotnet
#     host: containerapp

infra:
  provider: bicep
  path: infra
  module: main
