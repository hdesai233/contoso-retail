# CD to prod. Triggered on merge to main (dev) or manual promotion (test/prod).
# TODO: implement.
name: cd-prod
on:
  workflow_dispatch: {}


permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prod   # protection rules gate this in GitHub environment settings
    steps:
      - uses: actions/checkout@v4
      # TODO: az login (OIDC), az deployment group create, kubectl apply -k k8s/overlays/prod
