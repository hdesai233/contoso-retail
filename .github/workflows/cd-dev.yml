# CD to dev. Triggered on merge to main (dev) or manual promotion (test/prod).
# TODO: implement.
name: cd-dev
on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ['apps/**', 'infra/**', 'k8s/**']

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      # TODO: az login (OIDC), az deployment group create, kubectl apply -k k8s/overlays/dev
